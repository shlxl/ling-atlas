# frontend-app

- 阶段 2 示例：Next.js App Router 最小示例，消费 `/api` 的 nav/i18n/telemetry/search/graph。
- 页面包含 API readiness 卡片（nav/i18n/telemetry/manifest）+ nav 列表 + 搜索 demo + graph doc 概览（节点/边统计与示例）。
- `API_BASE`、`GRAPH_DOC` 可通过环境变量覆盖，默认指向 `http://127.0.0.1:8787/api` 与 `zh-graphrag-research`。
