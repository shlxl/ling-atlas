{
  "name": "ling-atlas",
  "private": true,
  "type": "module",
  "scripts": {
    "precheck": "node scripts/validate-frontmatter.mjs",
    "tags:normalize": "node scripts/normalize-tags.mjs",
    "stats:lint": "node scripts/stats-lint.mjs",
    "gen": "node scripts/pagegen.mjs",
    "build": "npm run gen && vitepress build docs",
    "dev": "npm run gen && vitepress dev docs",
    "md:lint": "markdownlint-cli2 'docs/**/*.md'",
    "build:search": "npm run embeddings:build && npm run build && npm run search:index && node -e \"import('node:fs').then(fs=>fs.cpSync('docs/public/pagefind','docs/.vitepress/dist/pagefind',{recursive:true}))\"",
    "check": "npm run precheck && npm run build",
    "preview": "vitepress preview docs",
    "search:index": "pagefind --site docs/.vitepress/dist --output-path docs/public/pagefind --force-language zh",
    "embeddings:build": "node scripts/build-embeddings.mjs",
    "dev:search": "vitepress build docs && npm run search:index && npm run embeddings:build && node -e \"import('node:fs').then(fs=>fs.cpSync('docs/public/pagefind','docs/.vitepress/dist/pagefind',{recursive:true}))\" && vitepress dev docs --host 0.0.0.0 --port 5173",
    "preview:search": "vitepress build docs && npm run search:index && npm run embeddings:build && node -e \"import('node:fs').then(fs=>fs.cpSync('docs/public/pagefind','docs/.vitepress/dist/pagefind',{recursive:true}))\" && vitepress preview docs --host 0.0.0.0 --port 4173"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "@sparticuz/chromium": "^141.0.0",
    "ajv": "^8.17.1",
    "cssnano": "^7.1.1",
    "fast-xml-parser": "^4.4.1",
    "globby": "^14.0.1",
    "gray-matter": "^4.0.3",
    "lightningcss": "^1.30.2",
    "markdown-link-check": "^3.14.1",
    "markdownlint-cli2": "^0.18.1",
    "marked": "^12.0.2",
    "pagefind": "^1.2.0",
    "pinyin-pro": "^3.22.2",
    "puppeteer": "^24.24.0",
    "sharp": "^0.34.4",
    "vitepress": "^1.5.0"
  }
}
